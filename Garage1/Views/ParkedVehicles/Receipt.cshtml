@model Garage1.ViewModel.ReceiptViewModel

@{
    ViewBag.Title = "Receipts";
}



<div class="data">
    <div class="data-date">
        <p>@DateTime.Now.ToString("dd/MM/yyyy")</p>
    </div>
    <div class="data-row">
        <p class="data-title">Garage Parking AB</p>
        <p>StreetName xx</p>
        <p>City</p>
    </div>
    <br />
    <div class="data-row">
        <div class="data-heading">@Model.Details.Vehicle.Manufacturer</div>
    </div>
    <br />
    <div class="data-row">
        <div class="data-title">Parked </div>
        <div class="data-body">@Model.Details.CheckInTime</div>
    </div>
    <br />
    <div class="data-row">
        <div class="data-title">Checked Out </div>
        <div class="data-body">@DateTime.Now</div>
    </div>

    <div class="data-row">
        <div class="data-title">@Html.DisplayNameFor(m => m.Details.Vehicle.Licens) </div>
        <div class="data-body">@Model.Details.Vehicle.Licens</div>
    </div>

    <div class="data-row">
        <div class="data-title">@Html.DisplayNameFor(m => m.Details.Vehicle.Model) </div>
        <div class="data-body">@Model.Details.Vehicle.Model</div>
    </div>

    <div class="data-row">
        <div class="data-title">@Html.DisplayNameFor(m => m.Details.Vehicle.Color) </div>
        <div class="data-body">@Model.Details.Vehicle.Color</div>
    </div>


    @{ 
        TimeSpan timeParked = DateTime.Now - Model.Details.CheckInTime;
    }

    <div class="data-row">
        <div class="data-title">Time Parked </div>
        <div class="data-body">
            @if (timeParked.Days > 0) { @timeParked.Days @Html.Raw("Days");} 
            @if (timeParked.Hours > 0) { @timeParked.Hours @Html.Raw(" Hours");} 
            @if (timeParked.Minutes > 0) { @timeParked.Minutes @Html.Raw(" Minutes");}
            @if (timeParked.Seconds > 0) { @timeParked.Seconds @Html.Raw(" Seconds"); }
        </div>
    </div>

    <div class="data-row">
        <div class="data-title">Cost </div>
        <div class="data-body text-danger"> <span>@(timeParked.TotalSeconds * 60) </span>kr</div>
    </div>

</div>

@Html.ActionLink("Back", "index", null, new { @class = "btn btn-primary"})